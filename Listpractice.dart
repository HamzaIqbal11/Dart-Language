void main() {
  List Studentname = ['Hamza', 'Khubaib', 'Ahmed'];
  // print(!Studentname.isEmpty);
  // Studentname.clear();
  // print(Studentname.isNotEmpty);
  // Studentname.sort((b, a) => -a.compareTo(b));
  Studentname.sort();
  Studentname.reversed;
  print(Studentname);
}
